<%# This file contains is the template for the API testing class %>
var ports = {
<% %> 
}
var base = "http://admin:admin@localhost:"
for (var i in ports){
  var frisby = require('frisby');
  var address=base.concat(ports[i])
  var answer = Number(i.replace("'",""))
  var check = "Check:"+ports[i]
  frisby.create(check)
    .get(address)
    .expectStatus(answer)
    .toss();
}



